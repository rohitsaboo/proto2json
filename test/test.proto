syntax = "proto3";

package test;

message ACCStream {
  repeated uint64 sensor_ns = 1;
  repeated float x = 2;
  repeated float y = 3;
  repeated float z = 4;
}

message Recording {
  ACCStream acc = 1;
}
